function FormControls() {
  console.error(
    'FormControls is not a component. It encompasses two components that should not be used directly (FormControlBorder and FormControlFooter).',
  );

  return null;
}

export default FormControls;
