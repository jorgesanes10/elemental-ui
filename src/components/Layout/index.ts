function Layout() {
  console.error(
    'The Layout component must not be rendered. Its sole purpose is ' +
      'to provide a Story. Use Container, Row and Col for the layout functionality.',
  );

  return null;
}

export default Layout;
